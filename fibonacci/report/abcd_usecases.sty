%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
% Author  :	Dimitrios Kouzapas
% Title	  :	Macros for usecases report
%
% Comments: Use this template, packages and macros to write reports for
%			the rest of	the usecases. Please see the contents of the file
%			to reduce conflicts
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
%                 Packages
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

\usepackage{comment}

\usepackage{xspace}
\usepackage{xcolor}


%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
%        Listing - packages and macros
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%


\usepackage{listings}

\newcommand*\lstnumberstyle{\ttfamily\scriptsize\textcolor{lightgray}}
\lstset{
	emph={
	local, global, protocol, choice, or, at, role, from, to, self,
		typestate, enum, end, %String, Code, Price, Travel
	},
	emphstyle={\color{blue}},
	numberstyle=\lstnumberstyle,
	basicstyle=\footnotesize\ttfamily,
	keywordstyle=\color{blue},
	breaklines=false,
	commentstyle=\color{purple}\ttfamily,
	language=Java,
	%numbers=left,
	captionpos=b,
	numbersep=4pt,
	mathescape=true
}%

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
%          Tikz - packages and macros
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

% Tikz packages
\usepackage{pgf}
\usepackage{tikz}
\usetikzlibrary{arrows,automata,positioning}
\usepackage[latin1]{inputenc}

\newenvironment{statemachine}[1][]
{
	\bgroup \begin{tikzpicture}[->,>=stealth',auto, semithick, #1]
}
{
	\end{tikzpicture} \egroup
}

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
%               Text macros
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

\newcommand{\Mungo}{\ensuremath{\mathsf{Mungo}}\xspace}


% References
\newcommand{\secref}[1]{Section~\ref{#1}}
\newcommand{\defref}[1]{Definition~\ref{#1}}
\newcommand{\lemref}[1]{Lemma~\ref{#1}}
\newcommand{\appref}[1]{Appendix~\ref{#1}}
\newcommand{\figref}[1]{Figure~\ref{#1}}
